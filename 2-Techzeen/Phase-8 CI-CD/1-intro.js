/**
 * =========================================================
 * ! JENKINS
 * =========================================================
 *
 * ---------------------------------------------------------
 * ! WHAT IS JENKINS?
 * ---------------------------------------------------------
 * Jenkins is an open-source automation server.
 *
 *
 * It helps developers automate tasks like:
 *
 * → Building code
 * → Testing code
 * → Deploying applications
 *
 *
 * Jenkins is mainly used in DevOps
 * for implementing CI/CD pipelines.
 *
 *
 * Simple Definition:
 *
 * Jenkins is a tool that automates
 * the software delivery process.
 *
 *
 * ---------------------------------------------------------
 * ! WHY DO WE USE JENKINS?
 * ---------------------------------------------------------
 * Without Jenkins:
 *
 * → Build manually
 * → Run tests manually
 * → Deploy manually
 *
 *
 * Problems:
 *
 * → Time consuming
 * → Human errors
 * → Slow releases
 *
 *
 * With Jenkins:
 *
 * → Everything can be automated
 *
 *
 * Benefits:
 *
 * → Faster development
 * → Faster testing
 * → Faster deployment
 * → Less manual work
 *
 *
 * ---------------------------------------------------------
 * ! JENKINS PLUGINS
 * ---------------------------------------------------------
 * One of the biggest strengths of Jenkins
 * is its huge plugin ecosystem.
 *
 *
 * Jenkins supports thousands of plugins.
 *
 *
 * Examples:
 *
 * → Git Plugin
 * → Docker Plugin
 * → Kubernetes Plugin
 * → Maven Plugin
 * → SonarQube Plugin
 * → AWS Plugin
 *
 *
 * Because of these plugins:
 *
 * Jenkins can integrate with almost
 * every DevOps tool.
 *
 *
 * ---------------------------------------------------------
 * ! MARKET USAGE
 * ---------------------------------------------------------
 * Jenkins is one of the most widely used
 * automation tools in the DevOps industry.
 *
 *
 * Reasons:
 *
 * → Free
 * → Open Source
 * → Huge community support
 * → Massive plugin ecosystem
 * → Easy integration with other tools
 *
 *
 * ---------------------------------------------------------
 * ! CI/CD EXPLAINED WITH JENKINS
 * ---------------------------------------------------------
 *
 * ! CI = Continuous Integration
 *
 * Continuous Integration means:
 *
 * Developers continuously merge code
 * into a shared repository.
 *
 *
 * Jenkins automatically:
 *
 * → Pulls latest code
 * → Builds application
 * → Runs tests
 * → Validates code
 *
 *
 * Goal:
 *
 * → Catch bugs early
 *
 *
 * ---------------------------------------------------------
 *
 * ! CD = Continuous Deployment / Delivery
 *
 * Continuous Deployment means:
 *
 * After successful testing,
 * the application is automatically deployed.
 *
 *
 * Jenkins automatically:
 *
 * → Takes latest working version
 * → Deploys it to server/environment
 *
 *
 * Goal:
 *
 * → Faster software releases
 *
 *
 * ---------------------------------------------------------
 * ! HOW JENKINS FITS INTO CI/CD?
 * ---------------------------------------------------------
 *
 * Developer Pushes Code
 *            ↓
 * GitHub / GitLab
 *            ↓
 * Jenkins Triggered
 *            ↓
 * Build Application
 *            ↓
 * Run Tests
 *            ↓
 * Generate Artifacts
 *            ↓
 * Deploy Application
 *            ↓
 * Application Live
 *
 *
 * ---------------------------------------------------------
 * ! WHAT IS A JENKINS PIPELINE?
 * ---------------------------------------------------------
 * Pipeline is a sequence of automated steps
 * executed by Jenkins.
 *
 *
 * Example Pipeline:
 *
 * Stage 1:
 * → Fetch Code
 *
 *
 * Stage 2:
 * → Build
 *
 *
 * Stage 3:
 * → Test
 *
 *
 * Stage 4:
 * → Deploy
 *
 *
 * Jenkins automates all these stages.
 *
 *
 * ---------------------------------------------------------
 * ! REAL WORLD EXAMPLE
 * ---------------------------------------------------------
 *
 * Developer pushes code to GitHub.
 *
 *
 * Jenkins automatically:
 *
 * → Detects new commit
 * → Downloads code
 * → Builds project
 * → Runs test cases
 * → Creates Docker image
 * → Deploys application
 *
 *
 * No manual intervention required.
 *
 *
 * ---------------------------------------------------------
 * ! BENEFITS OF JENKINS
 * ---------------------------------------------------------
 * → Open Source
 * → Free to use
 * → Huge plugin support
 * → Automates CI/CD
 * → Reduces manual effort
 * → Faster releases
 * → Easy integration with DevOps tools
 * → Large community support
 *
 *
 * ---------------------------------------------------------
 * ! INTERVIEW QUESTIONS
 * ---------------------------------------------------------
 *
 * Q1) What is Jenkins?
 *
 * Ans:
 * Jenkins is an open-source automation server
 * used to automate build, test, and deployment
 * processes in CI/CD pipelines.
 *
 *
 * Q2) Why is Jenkins used?
 *
 * Ans:
 * Jenkins automates software delivery tasks
 * such as building, testing, and deployment.
 *
 *
 * Q3) What is CI?
 *
 * Ans:
 * Continuous Integration is the practice of
 * automatically building and testing code
 * whenever developers push changes.
 *
 *
 * Q4) What is CD?
 *
 * Ans:
 * Continuous Deployment/Delivery is the process
 * of automatically deploying tested applications.
 *
 *
 * Q5) What is a Jenkins Pipeline?
 *
 * Ans:
 * A Jenkins Pipeline is a sequence of automated
 * stages such as build, test, and deployment.
 *
 *
 * Q6) Why is Jenkins popular?
 *
 * Ans:
 * Because it is open source, highly extensible,
 * and supports thousands of plugins.
 *
 *
 * Q7) Can Jenkins integrate with Docker and AWS?
 *
 * Ans:
 * Yes.
 * Jenkins supports integrations through plugins.
 *
 *
 * ---------------------------------------------------------
 * ! SHORT SUMMARY
 * ---------------------------------------------------------
 * → Jenkins = Automation Server
 * → Used for CI/CD
 * → Automates Build, Test, Deploy
 * → Supports thousands of plugins
 * → Pipeline = Sequence of automated stages
 * → One of the most popular DevOps tools
 *
 */