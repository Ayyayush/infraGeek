/**
 * ! SHORTCUTS IN LINUX
 *
 * it is one of the most important topics
 * these shortcuts make Linux easier and faster
 *
 *
 * Example:
 *
 * touch one two three
 *
 *
 * ! CURSOR MOVEMENT SHORTCUTS
 *
 * left/right arrow keys ke alawa:
 *
 * ctrl + a
 * - cursor ko line ke start pe le jata hai
 *
 *
 * ctrl + e
 * - cursor ko line ke end pe le jata hai
 *
 *
 * alt + b
 * - ek word backward move karta hai
 *
 *
 * alt + f
 * - ek word forward move karta hai
 *
 *
 * ! SEARCHING COMMAND HISTORY
 *
 * ctrl + r
 *
 * - command history search karta hai
 *
 *
 * Example:
 *
 * pehle:
 * nano mvn.srt
 *
 * chalaya tha
 *
 * ab:
 * ctrl + r
 *
 * aur mvn likho
 *
 * - matching command aa jayegi
 *
 *
 * ! CUTTING TEXT FROM CURSOR TO END
 *
 * touch one two three
 *
 * maan lo cursor two ke baad hai
 *
 * ctrl + k
 *
 * - current cursor position se end tak delete/cut ho jayega
 * - deleted text memory mein store ho jayega
 *
 *
 * ! PASTE CUT TEXT
 *
 * ctrl + y
 *
 * - previously cut text paste karta hai
 *
 *
 * ! DELETE FROM CURSOR TO START
 *
 * maan lo:
 *
 * touch one two three
 *
 * aur cursor two pe hai
 *
 * ctrl + u
 *
 * - current cursor position se line ke start tak delete ho jayega
 * - deleted text memory mein save rahega
 *
 *
 * ! DELETE WORD AFTER CURSOR
 *
 * alt + d
 *
 * - current cursor position ke baad wala word delete karta hai
 *
 *
 * ! DELETE WORD BEFORE CURSOR
 *
 * Example:
 *
 * touch one two tttttthree four five
 *
 * maan lo cursor current word ke beech mein hai
 *
 * ctrl + w
 *
 * - cursor se pehle wala word delete karta hai
 *
 *
 * ! TERMINAL SHORTCUTS
 *
 * ctrl + l
 *
 * - terminal clear karta hai
 *
 *
 * clear
 *
 * - same task karta hai
 *
 *
 * ctrl + d
 *
 * - EOF bhejta hai
 * - shell empty ho toh terminal/session close bhi ho sakta hai
 *
 *
 * ! OPEN TERMINAL SHORTCUT
 *
 * ctrl + alt + t
 *
 * - terminal open karta hai
 *
 *
 * ! TERMINAL ZOOM
 *
 * ctrl + shift + +
 *
 * - terminal zoom in
 *
 *
 * ctrl + -
 *
 * - terminal zoom out
 *
 *
 * ! AUTO COMPLETE
 *
 * cat mvn.srt
 *
 * tab press karne se:
 *
 * - auto complete ho sakta hai
 *
 *
 * Example:
 *
 * cd Down
 *
 * tab
 *
 * - Downloads autocomplete ho jayega
 *
 *
 * agar multiple options hain:
 *
 * double tab
 *
 * - saare suggestions show karega
 *
 *
 * ! IMPORTANT CORRECTION
 *
 * Original notes mein:
 *
 * alt + tab se auto complete
 *
 * ye incorrect tha
 *
 *
 * correct:
 *
 * tab
 * - autocomplete
 *
 * alt + tab
 * - window/application switching shortcut hota hai
 *
 *
 * ! IMPORTANT NOTES
 *
 * ctrl + a
 * - line start
 *
 * ctrl + e
 * - line end
 *
 * ctrl + k
 * - cut till end
 *
 * ctrl + u
 * - cut till beginning
 *
 * ctrl + y
 * - paste cut text
 *
 * ctrl + r
 * - history search
 *
 * tab
 * - autocomplete
 *
 *
 * ! SUMMARY OF THIS LECTURE
 *
 * Shortcuts learned:
 *
 * ctrl + a
 * ctrl + e
 * alt + b
 * alt + f
 * ctrl + r
 * ctrl + k
 * ctrl + y
 * ctrl + u
 * alt + d
 * ctrl + w
 * ctrl + l
 * ctrl + d
 * ctrl + alt + t
 * tab
 *
 *
 * Concepts learned:
 * - cursor movement
 * - history search
 * - line editing
 * - text deletion
 * - autocomplete
 * - terminal control
 *
 *
 * ! INTERVIEW QUESTIONS
 *
 * 1) What does ctrl + a do in Linux terminal?
 * Answer:
 * - Moves cursor to beginning of line.
 *
 *
 * 2) What does ctrl + e do?
 * Answer:
 * - Moves cursor to end of line.
 *
 *
 * 3) What does ctrl + r do?
 * Answer:
 * - Searches command history.
 *
 *
 * 4) What does ctrl + k do?
 * Answer:
 * - Cuts text from cursor to end of line.
 *
 *
 * 5) What does ctrl + u do?
 * Answer:
 * - Cuts text from cursor to beginning of line.
 *
 *
 * 6) What does ctrl + y do?
 * Answer:
 * - Pastes previously cut text.
 *
 *
 * 7) What does alt + d do?
 * Answer:
 * - Deletes word after cursor.
 *
 *
 * 8) What does ctrl + w do?
 * Answer:
 * - Deletes word before cursor.
 *
 *
 * 9) What does ctrl + l do?
 * Answer:
 * - Clears terminal screen.
 *
 *
 * 10) What is the use of tab key in Linux?
 * Answer:
 * - Autocomplete files/directories/commands.
 *
 */